using UnityEngine;

public class Player3D : MonoBehaviour
{
    // Awake is called before any Start method is called on any object
    void Awake()
    {
    }


    // OnEnable is called after Awake and before Start.
    // OnEnable is also called anytime the object is enabled at runtime
    void OnEnable()
    {
    }

    // OnDisable is called when the object is disabled at runtime
    void OnDisable()
    {
    }
    

    // Start is called before the first frame update
    void Start()
    {
    }


    // Update is called once per frame
    void Update()
    {
    }
    
    // FixedUpdate is called once per physics update (By default exactly 50 times per second)
    void FixedUpdate()
    {
    }


    // OnDrawGizmos is called only in the editor, every frame the object is in the scene
    private void OnDrawGizmos()
    {
    }

    // OnDrawGizmosSelected is called only in the editor, every frame the object is selected in the scene
}
